#include <bits/stdc++.h>
using namespace std;

int main() {
    string s = "ritvik";
    cout << s.size() << "\n";

    //checking if i can sort the string

    sort(s.begin(), s.end());
    cout<<s<<"\n";
    return 0;
}